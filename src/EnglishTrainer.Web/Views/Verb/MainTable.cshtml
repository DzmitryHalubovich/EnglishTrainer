@model EnglishTrainer.Web.Models.VerbIndexViewModel

@{
    ViewData["Title"] = "MainTable";
}

<p>
    <a asp-action="MainPage" asp-controller="Verb">Back to main page</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Infinitive
            </th>
            <th>
                PastSimple
            </th>
            <th>
                PastParticiple
            </th>
            <th>
                TranslateRu
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.VerbsList)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Infinitive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PastSimple)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PastParticiple)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TranslateRu)
            </td>
        </tr>
}
        <form method="get">
            <input type="hidden" name="pageOptions.CurrentPage" value="@(Model.Options.PageOptions.CurrentPage - 1)" />
            @if (Model.Options.PageOptions.HasPreviousPage)
            {
                <input class="btn btn-dark btn-sm" type="submit" value="Prev" />
            }
            else
            {
                <input class="btn btn-dark btn-sm" disabled type="submit" value="Prev" />
            }
        </form>
        <form method="get">
            <input type="hidden" name="pageOptions.CurrentPage" value="@(Model.Options.PageOptions.CurrentPage + 1)" />
            @if (Model.Options.PageOptions.HasNextPage)
            {
                 <input class="btn btn-dark btn-sm" type="submit" value="Next" />
            }
            else
            {
                 <input class="btn btn-dark btn-sm" disabled type="submit" value="Next" />
            }
        </form>
            </tbody>

</table>

